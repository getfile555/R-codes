# Aim: To find the Estimation
X <- c(5, 6, 7, 8, 9, 10)
mean(X)
var(X)

data <- c(12, 15, 18, 20, 22, 16, 14, 21, 17)

Sample_mean <- mean(data)

cat("The point estimation for population mean:", Sample_mean, "\n")
